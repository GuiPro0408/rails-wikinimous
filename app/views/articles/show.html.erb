<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <article>
        <h1>
          <%= @article.title %>
        </h1>
        <hr>
        <p><%= @article.content %></p>
      </article>
      <%= link_to("Edit", edit_article_path, class: 'btn btn-primary') %>
      <%= link_to("Index", articles_path, class: 'btn btn-warning') %>
    </div>
  </div>
</div>
